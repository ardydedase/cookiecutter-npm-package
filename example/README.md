# Sample usage

Ensure that current working directory is the `example` folder.

    cd example

Install dependencies.

    npm install

Use with JavaScript.

    node index.js

Use with TypeScript.

    # Compile
    node_modules/.bin/tsc
    # Run
    node dist/index.js
